// Dependencies
import { ReactNode } from "react";

export type CalculatorElements = {
    heigthInputElement: ReactNode;
    weigthInputElement: ReactNode;
    actionButtonElement: ReactNode;
};

export type CalculatorProps = CalculatorElements;
